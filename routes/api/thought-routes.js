// GET all thoughts at `/api/thoughts`
// GET to get a single thought by its _id
// POST to create a new though
//    (dont forget to push the created thought's _id to associated 
//     USERS thought array field)